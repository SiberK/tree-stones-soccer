/**
 * src/debug.ts
 *
 * Тумблеры отладочного вывода. Нужны, чтобы СПРЯТАТЬ шумный вывод,
 * а не удалять его: при отладке ядра (углы ±180°, диапазон сил, детекция гола)
 * эти логи незаменимы, но в обычной игре они сыплют десятками строк на ход.
 *
 * Безопасность: все эти флаги управляют ТОЛЬКО console.log. Оборачивание
 * лога в `if (DEBUG_*)` не меняет ни одного return и ни одной переменной —
 * значит на механику игры это не влияет НИКАК. (Аргументы логов — чистое
 * форматирование чисел, без побочных эффектов, поэтому под if они просто
 * не вычисляются — это ещё и чуть быстрее.)
 *
 * Включаются пересборкой (поменять false на true и `npx tsc`). Позже, если
 * захочется чекбокс в настройках — перенесём нужные флаги в state.ts и
 * привяжем к UI; сейчас отдельный файл чище и не раздувает state.
 */

// Мастер-выключатель: если false — все семейные тумблеры ниже считаются
// выключенными независимо от их значения. Удобно «заткнуть всё» одной строкой.
export let DEBUG_AI = false;

// Семейство логов голевого анализа: [GOAL DEBUG], [GOAL MISS], [GOAL REJECT].
// Самое шумное — сыплет на каждый узел перебора углов/сил.
export let DEBUG_GOAL = false;

// Топ кандидатов и их оценки: [AI CANDIDATES] + топ-5 в findBestMoveFromCandidates.
// Терпимо по объёму и полезно, чтобы видеть, почему бот выбрал именно этот ход.
export let DEBUG_CANDIDATES = false;